extends ../../_layout/default

block view

  .container-fluid

    .row

      .col-12

        .card.mb-4

          .card-header.d-flex.justify-content-between.align-items-center

            strong Race Results

            button.btn.btn-primary(
              type='button'
              id='btnAddResult'
            )
              i.fa.fa-plus.me-2
              | Add Result

          .card-body

            div.d-none.text-center.mb-3(
              id='resultLoading'
            )
              .spinner-border.text-primary

            .row.mb-3

              .col-md-4

                input.form-control(
                  type='text'
                  id='searchResult'
                  placeholder='Search Event, Program, Team or Medal'
                )

            .table-responsive

              table.table.table-striped.table-hover

                thead

                  tr

                    th Event
                    th Program
                    th Team
                    th Distance
                    th Duration
                    th Avg Speed
                    th Position
                    th Points
                    th Medal
                    th Actions

                tbody#resultsTableBody

            .row.mt-3

              .col-md-6

                button.btn.btn-secondary(
                  type='button'
                  id='btnRefreshResults'
                )
                  | Refresh

              .col-md-6.text-end

                button.btn.btn-outline-primary.me-2(
                  type='button'
                  id='btnPreviousPage'
                )
                  | Previous

                span#paginationInfo.mx-2
                  | Page 1

                button.btn.btn-outline-primary(
                  type='button'
                  id='btnNextPage'
                )
                  | Next

  .modal.fade(
    id='resultModal'
    tabindex='-1'
    aria-hidden='true'
  )

    .modal-dialog.modal-lg

      .modal-content

        .modal-header

          h5.modal-title
            | Race Result

          button.btn-close(
            type='button'
            data-coreui-dismiss='modal'
          )

        .modal-body

          input(
            type='hidden'
            id='resultId'
          )

          .row.g-3

            .col-md-4

              label.form-label Competition Event

              select.form-select(
                id='eventId'
              )

            .col-md-4

              label.form-label Program / Discipline

              select.form-select(
                id='programId'
              )

                option(value='') Select Event First

            .col-md-4

              label.form-label Team

              select.form-select(
                id='participantId'
              )

                option(value='') Select Program First

            .col-md-4

              label.form-label Competition Date

              input.form-control(
                type='date'
                id='competitionDate'
              )

            .col-md-4

              label.form-label Start Time

              input.form-control(
                type='time'
                id='startTime'
              )

            .col-md-4

              label.form-label End Time

              input.form-control(
                type='time'
                id='endTime'
              )

            .col-md-4

              label.form-label Duration (Minutes)

              input.form-control(
                type='number'
                id='durationMinutes'
                readonly
              )

            .col-md-4

              label.form-label Distance (KM)

              input.form-control(
                type='number'
                min='0'
                step='0.01'
                id='distanceKm'
              )

            .col-md-4

              label.form-label Average Speed (KM/H)

              input.form-control(
                type='number'
                id='avgSpeedKmh'
                readonly
              )

            .col-md-4

              label.form-label Maximum Speed (KM/H)

              input.form-control(
                type='number'
                min='0'
                step='0.01'
                id='maxSpeedKmh'
              )

            .col-md-4

              label.form-label Position

              input.form-control(
                type='number'
                min='1'
                id='position'
              )

            .col-md-4

              label.form-label Points

              input.form-control(
                type='number'
                min='0'
                id='points'
              )

            .col-md-12

              label.form-label Medal

              input.form-control(
                id='medal'
                readonly
              )

          .mt-3.text-danger(
            id='resultFormError'
          )

        .modal-footer

          button.btn.btn-secondary(
            type='button'
            data-coreui-dismiss='modal'
          )
            | Cancel

          button.btn.btn-success(
            type='button'
            id='btnSaveResult'
          )
            | Save Result

  .modal.fade(
    id='deleteResultModal'
    tabindex='-1'
    aria-hidden='true'
  )

    .modal-dialog

      .modal-content

        .modal-header

          h5.modal-title
            | Delete Result

          button.btn-close(
            type='button'
            data-coreui-dismiss='modal'
          )

        .modal-body

          p
            | Are you sure you want to delete this race result?

          input(
            type='hidden'
            id='deleteResultId'
          )

        .modal-footer

          button.btn.btn-secondary(
            type='button'
            data-coreui-dismiss='modal'
          )
            | Cancel

          button.btn.btn-danger(
            type='button'
            id='btnConfirmDeleteResult'
          )
            | Delete Result

append pageScripts

  script(src='js/race-results.js')